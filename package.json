{
  "name": "netlify-functions-api",
  "version": "1.0.0",
  "description": "Netlify serverless function — MoonPay webhook + static frontend",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "express": "^4.18.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
